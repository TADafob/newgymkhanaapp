import 'package:flutter/material.dart';

const Color primaryColor = Color(0xFF00B8D4); // Blue
const Color secondaryColor = Color(0xFFFFC107); // Yellow
const Color accentColor = Color(0xFFE91E63); // Pink (for highlights)
const Color backgroundColor = Colors.white;
const Color textColor = Colors.black;